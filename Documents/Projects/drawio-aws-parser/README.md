# drawio-aws-parser

Parser de **draw.io/diagrams.net** para extraer **VPC**, **Subnets**, **Servicios AWS** y **Conexiones** en un JSON.

## Instalación
```bash
npm i -g .
# o localmente en tu proyecto
npm i drawio-aws-parser